package com.facebook.react

import org.gradle.api.Plugin
import org.gradle.api.Project

class ReactRootProjectPlugin : Plugin<Project> {
    override fun apply(project: Project) {
        // Plugin logic here (can be empty for now)
    }
}
